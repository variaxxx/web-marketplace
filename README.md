# Web Marketplace

Nx monorepo with microservice NestJS backend & Angular frontend

## Setup

```
npx nx run-many --target=serve
```

generate tokens like this
```
openssl rand -hex 40
```

linting
```
npx nx run-many --target=lint --all --fix
```
