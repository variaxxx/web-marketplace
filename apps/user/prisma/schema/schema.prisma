generator client {
  provider = "prisma-client-js"
  output   = "../../../../node_modules/@prisma/generated/userClient"
}

datasource db {
  provider = "postgresql"
  url      = env("USER_DATABASE_URL")
}
